Spark sandbox worker — runs the 6-stage pipeline against sandbox_ tables only.
Deploy on Render as a Background Worker (or Railway service).
Build: npm install     Start: npm start
Env needed: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, GEMINI_API_KEY,
VITE_OPENAI_API_KEY, OPENAI_IMAGE_MODEL, SANDBOX_BUCKET=brand-headers
